\documentclass{article}
\usepackage[T2A]{fontenc}
\usepackage[cp1251]{inputenc}
\usepackage[english,russian]{babel}
\usepackage[tbtags]{amsmath}
\usepackage{amsfonts,amssymb,mathrsfs,amscd}
%для подключения графики используются стандартные команды, но кроме файла *.eps
%необходимо наличие в текущей директории соответствующего файла *.pdf
%-------------------------------------------------

%\usepackage{fancyhdr}
%\pagestyle{fancy}
%\fancyhead{}
%\fancyhead[LE,RO]{\thepage}
%\fancyhead[CO]{текст-центр-нечетные}
%\fancyhead[LO]{текст-слева-нечетные}
%\fancyhead[CE]{текст-центр-четные}
%\fancyfoot{}

\usepackage[hyper]{msb-a}
\Volume10

%\JournalName{Дагестанские электронные математические известия}
\JournalName{Дагестанские Электронные Математические Известия}
%\JournalName{}
%Пустой аргумент приводит к исчезновению всех атрибутов журнала "Математический
%сборник", файл можно представить в любой другой журнал
%-------------------------------------------------
\numberwithin{equation}{section}
%-------------------------------------------------
\theoremstyle{plain}
\newtheorem{theorem}{Теорема}[section]
\newtheorem{lemma}{Лемма}[section]
\newtheorem{propos}{Предложение}
%-------------------------------------------------
\theoremstyle{definition}
\newtheorem{definition}{Определение}
\newtheorem{proof}{Доказательство}\def\theproof{}
\newtheorem{remark}{Замечание}
%-------------------------------------------------
\def\Re{\operatorname{Re}}
\def\Im{\operatorname{Im}}
\def\const{\operatorname{const}}
\def\RR{\mathbb R}
\def\CC{\mathbb C}
\def\NN{\mathbb N}
\def\RS{\mathfrak R}
\def\bz{\mathbf z}
\def\sH{\mathscr H}
\def\HH{\mathscr H}
\def\mro{\widehat\rho}
%-------------------------------------------------

\setcounter{page}{22}

\begin{document}

\title{Сплайны по рациональным интерполянтам}

\author[A.-R.\,K.~Ramazanov]{A.-Р.\,К.~Рамазанов}
\address{Дагестанский государственный университет,
Дагестанский научный центр РАН}

\email{ar-ramazanov@rambler.ru}

%второй автор

\author[V.\,G.~Magomedova]{В.\,Г.~Магомедова}
\address{Дагестанский государственный университет}
\email{vazipat@rambler.ru}

\date{01.12.2015}
\udk{517.5}

\maketitle


\begin{fulltext}

\begin{abstract}

Для непрерывной на данном отрезке (или периодической) функции построены
$n$--точечные ($n=2,3,4$) рациональные интерполянты и по ним интерполяционные
рациональные сплайны. Последовательности сплайнов по $n$--точечным интерполянтам
при $n=2$ и $n=3$ для любой последовательности сеток с диаметром, стремящимся к нулю,
равномерно на всем отрезке сходится к самой функции. При $n=3$ этим свойством безусловной
сходимости обладают первые производные, а при $n=4$ --- первые и вторые производные.

Даны также оценки скорости сходимости.


Библиография: 16 названий.
$$  $$

For a continuous function on a given interval (or periodic)  a  $n$--point
  ($n=2,3,4$)  rational interpolant  is constructed. Rational splines  by means
 of \linebreak interpolants are constructed.
The sequences of the splines with respect to the n-point interpolant for $n = 2$
 and  $n=3$ for each sequence of grids with a diameter tending to zero uniformly
on the entire interval converges to the function itself. For $n= 3$  first derivatives
  have this  unconditional convergence property, and for $n = 4$  --- first and second
derivatives.

We also give estimates of the convergence rate.

Bibliography: 16 items.
\end{abstract}

\begin{keywords}
сплайны, интерполяционные, рациональные сплайны, безусловная сходимость.

{\bf Keywords:} splines, interpolation, rational splines, unconditional convergence.

\end{keywords}

\markright{Сплайны по рациональным интерполянтам}




%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section*{Введение}
\label{subsec0}


Вопросы сходимости различных видов полиномиальных сплайнов в классических функциональных пространствах
исследованы в завершенной форме (см., напр., \cite{1}--\cite{9}
и список литературы в них).

Изучаются также (см. \cite{10}--\cite{15} и цитирование в них) аппроксимативные свойства
рациональных сплайнов специального вида при ограничениях типа монотонности,
выпуклости или сохранения знака.

В данной работе построены $n$--точечные рациональные интерполянты для значений
$n=2,3,4$, на их основе построены интерполяционные рациональные сплайны и получены оценки
скорости сходимости этих сплайнов и их производных до допустимых порядков.

\section{Сплайны по двуточечным рациональным интерполянтам}
\label{subsec1}

Пусть функция $f(x)$ непрерывна на отрезке $[a,b]$, на котором задана некоторая сетка узлов
$\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 1)$.

Всюду ниже положим $h_k=x_k-x_{k-1}$ $(k=1,2,\dots,N)$, $\|\Delta\|=\max\{h_k:k=1,2,\dots,N\}$.

Для каждой пары узлов $x_{k-1}<x_k$ $(k=1,2,\dots,N)$ на отрезке $[x_{k-1}, x_k]$
рассмотрим рациональную функцию вида
\begin{equation}\label{1.1}
q_k(x)=q_k(x,H)=a_k+\frac{A_k}{x-u_k}
\end{equation}
с $u_k=x_k+H$ при $H>b-a$ такую, что $q_k(x_j)=f(x_j)$ при $j=k-1,k$.

Из этих условий получим
\begin{equation}\label{1.2}
A_k=-f(x_{k-1}, x_k)(x_{k-1}-u_k)(x_k-u_k),\, a_k=f(x_k)+f(x_{k-1},x_k)(x_{k-1}-u_k);
\end{equation}
здесь $f(x_{k-1},x_k)$ означает разделенную разность функции $f(x)$ в точках $x_{k-1}$, $x_k$.

Значит, рациональная функция \eqref{1.1} с $u_k=x_k+H$ и коэффициентами \eqref{1.2}
представляет собой для функции $f(x)$ двуточечный интерполянт, непрерывный на отрезке
$[x_{k-1}, x_k]$.

При этом в силу монотонности $q_k(x)$ на $[x_{k-1}, x_k]$ и условий $q_k(x_j)=f(x_j)$
$(j=k-1,k)$ имеем
\begin{equation}\label{1.3}
|q_k(x)-f(x)|\leqslant \omega (h_k, f)\quad (x\in [x_{k-1}, x_k]).
\end{equation}

Построим теперь по сетке узлов $\Delta$  непрерывную на отрезке $[a,b]$ кусочно--ра\-циональную
функцию $Q_N(x)$ такую, что $Q_N(x)=q_k(x)$ при $x\in [x_{k-1}, x_k]$ $(k=1,2,\dots, N)$.

Тогда для непрерывной на $[a,b]$ функции $f(x)$ с использованием \eqref{1.3}
получим неравенство
\begin{equation}\label{1.4}
|Q_N(x)-f(x)|\leqslant \omega (\|\Delta\|, f)\quad (x\in [a,b]).
\end{equation}

Пусть теперь $f\in C^{(1)}[a,b]$, $u_k=x_k+H$ для наперед заданного числа $H>b-a$.

Тогда при $x\in [x_{k-1}, x_k]$ $(k=1,2,\dots, N)$ имеем
$$
q^\prime_k(x)=-\frac{A_k}{(x-u_k)^2}=f(x_{k-1}, x_k)
\frac{(x_{k-1}-u_k)(x_k-u_k)}{(x-u_k)^2}=
$$
$$
=f^\prime(\xi)\frac{(x_{k-1}-u_k)(x_k-u_k)}{(x-u_k)^2}
$$
для некоторой точки $\xi\in(x_{k-1}, x_k)$.


Значит, для этих точек $x, \xi\in [x_{k-1}, x_k]$ получим
$$
|q_k^\prime (x)-f^\prime (x)|=[f^\prime (\xi)-f^\prime (x)]+
f^\prime (\xi)
\left[\frac{(x_{k-1}-u_k)(x_k-u_k)}{(x-u_k)^2} -1\right].
$$
Отсюда
$$
|q^\prime_k(x)-f^\prime (x)|\leqslant \omega (h_k, f^\prime)+
|f^\prime (\xi)|\left[1-\frac{(u_k-x_{k-1})(u_k-x_k)}{(u_k-x_{k-1})^2}\right]=
$$
$$
=\omega (h_k, f^\prime)+|f^\prime (\xi)|\frac{h_k}{u_k-x_{k-1}};
$$
\begin{equation}\label{1.5}
|q_k^\prime(x)-f^\prime (x)|\leqslant \omega(h_k, f^\prime)+
\frac 1H \sup_{x_{k-1}\leqslant x\leqslant x_k} |f^\prime (x)|\cdot h_k.
\end{equation}

Пусть для краткости $j=k$, если $x\in \left[\frac 12 (x_{k-1}+x_k), x_k\right]$,
и $j=k-1$, если $x\in \left[x_{k-1}, \frac 12 (x_{k-1}+x_k)\right]$.
Тогда найдется точка $\xi_1$ между точками $x$ и $x_j$ такая, что
$$
|q_k(x)-f(x)|=|[q_k(x)-f(x)]-[q_k(x_j)-f(x_j)]|=
$$
$$
=|q_k^\prime (\xi_1)-f^\prime (\xi_1)|\,|x-x_j|\leqslant |q_k^\prime (\xi_1)-f^\prime (\xi_1)|
\frac{h_k}2.
$$
Отсюда и из \eqref{1.5} при $x\in [x_{k-1}, x_k]$ получим
\begin{equation}\label{1.6}
|q_k(x)-f(x)|\leqslant \frac 12 h_k \omega (h_k, f^\prime)+\frac 1{2H}
\sup_{x_{k-1}\leqslant x\leqslant x_k} |f^\prime (x)|\cdot h_k^2.
\end{equation}

Из \eqref{1.5} и \eqref{1.6} вытекает, что если $f\in C^{(1)}[a,b]$, $H$ ---
любое наперед заданное число, большее $b-a$, $Q_N(x)$ --- кусочно--рациональная по сетке
$\Delta$ функция с $Q_N(x)=q_k(x, H)$ при $x\in [x_{k-1}, x_k]$ $(k=1,2,\dots,N)$, то
при всех натуральных $N$ выполняются неравенства
$$
\sup_{a\leqslant x\leqslant b} |Q_N^\prime (x\pm 0)-f^\prime (x)|\leqslant
 \omega (\|\Delta\|, f^\prime)+\frac{\|f^\prime\|}{H}\|\Delta\|,
$$
$$
\sup_{a\leqslant x\leqslant b} |Q_N (x)-f (x)|\leqslant
\frac{\|\Delta\|} 2 \omega (\|\Delta\|, f^\prime)+\frac{\|f^\prime\|}{2H}\|\Delta\|^2;
$$
здесь $\|f^\prime\|=\sup\limits_{a\leqslant x\leqslant b}|f^\prime(x)|$.

\section{Сплайны по 3--точечным рациональным интерполянтам}
\label{subsec2}

Пусть функция $f(x)$ непрерывна на отрезке $[a,b]$, на котором задана произвольная сетка узлов
$\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 2)$.

Тогда справедливы следующие две леммы.

\begin{lemma}\label{lemma2.1} Для любой тройки узлов $x_{i-1}<x_i<x_{i+1}$
$(i=1,2,\dots,{N-1})$ и любого фиксированного числа $g_i\not \in [x_{i-1}, x_{i+1}]$
существует (единственная) непрерывная на отрезке $[x_{i-1}, x_{i+1}]$
рациональная функция вида
\begin{equation}\label{2.1}
R_i (x)=\alpha_i+\beta_i(x-x_i)+\frac{\gamma_i}{x-g_i}
\end{equation}
такая, что
$$
R_i(x_j)=f(x_j)\quad (j=i-1,i,i+1).
$$
 \end{lemma}

Если для различных между собой точек $t_1, t_2, t_3\in [a,b]$ разделенные
разности первого и второго порядков функции $f(x)$ в них обозначить соотвественно через $f(t_1, t_2)$
и $f(t_1,t_2,t_3)$, то непосредственно легко проверить, что искомой рациональной функцией
 служит \eqref{2.1}
при $g_i \not \in [x_{i-1}, x_{i+1}]$ и следующих значениях
коэффициентов $\alpha_i, \beta_i, \gamma_i$:
\begin{equation}\label{2.2}
\begin{matrix}
\alpha_i=f(x_i)-f(x_{i-1}, x_i, x_{i+1})(x_{i-1}-g_i)(x_{i+1}-g_i),\\
\beta_i=f(x_{i-1}, x_{i+1})+f(x_{i-1}, x_i, x_{i+1})(x_i-g_i),\\
\gamma_i=f(x_{i-1}, x_i, x_{i+1})(x_{i-1}-g_i)(x_i-g_i)(x_{i+1}-g_i).
\end{matrix}
\end{equation}

\begin{lemma}\label{lemma2.2} Если для данной тройки узлов $x_{i-1}< x_i< x_{i+1}$
$(i=1,2,\dots,$ $N-1)$ выбрать
\begin{equation}\label{2.3}
g_i=\begin{cases}
2x_{i+1}-x_i \quad\text{ при }\quad x_{i+1}-x_i\leqslant x_i-x_{i-1},\\
2x_{i-1}-x_i \quad \text{ при }\quad x_{i+1}-x_i> x_i-x_{i-1},
\end{cases}
\end{equation}
то для непрерывной на отрезке $[a,b]$ функции $f(x)$ и рациональной функции \eqref{2.1}
 с коэффициентами \eqref{2.2} при $x\in [x_{i-1}, x_{i+1}]$
выполняется неравенство
\begin{equation}\label{2.4}
|f(x)-R_i(x)|\leqslant 19 \,\,\omega (\delta, f),
\end{equation}
где $\delta=\max\{h_i, h_{i+1}\}$.
\end{lemma}
Переходим к построению   кусочно--рациональной функции
 $R_{N,k}(x)=R_{N,k}(x; f)$  на отрезке $[a,b]$ для данных натуральных чисел $N\geqslant 2$ и $k$.
Возьмем рациональные  функции  $R_i (x)$ $(i=1,2,\dots,N-1)$ из \eqref{2.1},
которые удовлетворяют условиям \eqref{2.2} и \eqref{2.3}; кроме того, будем считать
$R_0(x)\equiv R_1(x)$, $R_N (x)\equiv R_{N-1}(x)$
(или в случае непрерывной $(b-a)$--периодической функции $f(x)$ сетку узлов $\Delta$
будем считать продолженной $(b-a)$--периодически).

При $x\in [x_{i-1}, x_i]$ $(i=1,2,\dots,N)$ положим
\begin{equation}\label{2.6}
R_{N, k}(x)=\frac{R_i(x) (x-x_{i-1})^k+R_{i-1} (x) (x_i-x)^k}
{(x-x_{i-1})^k+(x_i-x)^k}.
\end{equation}

Значит, на каждом отрезке $[x_{i-1}, x_i]$ $(i=1,2,\dots, N)$
функция  $R_{N,k}(x)$ представляет собой некоторую рациональную функцию с отличным
от нуля знаменателем.

При $k=1$ равенство \eqref{2.6} приобретает форму
$$
R_{N,1} (x)=R_i (x) \frac{x-x_{i-1}}{x_i-x_{i-1}}+R_{i-1} (x)\frac{x_i-x}{x_i-x_{i-1}}.
$$
Если при этом вместо рациональных функций $R_i(x)$ брать полиномы второй степени,
интерполирующие $f(x)$ в узлах $x_{i-1}, x_i, x_{i+1}$, то вместо $R_{N,1}(x)$ получим
кубический сплайн (дефекта 2).


Следующее утверждение лежит в основе безусловной сходимости рациональных сплайнов.
Оно вытекает из лемм \ref{lemma2.1} и \ref{lemma2.2}.

\begin{theorem}\label{teor2.1}
 Пусть данное натуральное число $N\geqslant 2$ и на отрезке
$[a,b]$ задана произвольная сетка узлов $\Delta: a=x_0<x_1<\dots<x_N=b$.

Тогда для любой непрерывной на отрезке $[a,b]$ функции $f(x)$ и интерполяционного
рационального сплайна $R_{N,k} (x)=R_{N,k}(x;f)$
при любом натуральном $k$ и всех $x\in [a,b]$ выполняется неравенство
$$
|f(x)-R_{N,k}(x)|\leqslant 19\,\omega (\|\Delta\|,f).
$$
 \end{theorem}

В работе \cite{8} построен пример функции из класса $\text{Lip}\,\frac 13$, для которой
существует расходящаяся последовательность интерполяционных кубических
сплайнов.

В работах \cite{2}, \cite{3} доказано, что для любого $\alpha$ $(0<\alpha<1)$ существует функция
из класса $\text{Lip}\,\alpha$ и последовательность сеток с диаметром
разбиения, стремящимся к нулю, для которых соответствующая последовательность
интерполяционных параболических или кубических сплайнов расходится.


Следуя Ю.Н. Субботину (\cite{16}), будем говорить, что интерполяционные \linebreak сплайны безусловно
сходятся к данной функции, если для любой последовательности сеток с диаметром,
стремящимся к нулю, соответствующая последовательность интерполяционных сплайнов
 равномерно сходится к этой функции. Вполне аналогично определяется безусловная сходимость
для производных функций и соответствующих сплайнов.

Значит, безусловная сходимость интерполяционных  кубических
или параболических сплайнов  для всех функций класса не свойственна ни одному
из классов $\text{Lip}\,\alpha$ при $0<\alpha<1$.
При этом (\cite{2}, \cite{3}, \cite{9}) для непрерывной периодической функции имеет место безусловная
сходимость интерполяционных параболических (кубических) сплайнов тогда и только тогда,
когда функция принадлежит классу $\text{Lip}\,1$.

Известно также (\cite{1}--\cite{3}), что для производной $f^{(i)} (x)$ каждой функции из класса
$C^{(i)}[a,b]$ при $i=1,2$ имеет место безусловная сходимость соответствующих производных
интерполяционных параболических и кубических сплайнов.

Рассмотрим теперь вопрос скорости сходимости рациональных сплайнов \linebreak
$R_{N,k}(x; f)$
к непрерывно дифференцируемой на отрезке $[a,b]$ функции $f(x)$. При этом
для произвольно заданной сетки узлов $\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 2)$ будем
придерживаться принятых выше обозначений.

\begin{lemma}\label{lemma2.3} Если для тройки узлов $x_{i-1}<x_i<x_{i+1}$ ${(i=1,2,\dots,N-1)}$ значение
$g_i$ выбрано по \eqref{2.3}, то для непрерывно дифференцируемой на отрезке $[a,b]$
функции $f(x)$ и рациональной функции \eqref{2.1} с коэффициентами \eqref{2.2} при $x\in [x_{i-1}, x_{i+1}]$
выполняется неравенство
$$
|f(x)-R_i (x)|\leqslant 16\, \delta \omega (\delta, f^\prime),
$$
где $\delta=\max \{h_i, h_{i+1}\}$.
\end{lemma}

Из леммы \ref{lemma2.3} вытекает
\begin{theorem}\label{teor2.2} Пусть натуральное $N\geqslant 2$  и задана произвольная
сетка узлов $\Delta: a=x_0<x_1<\dots<x_N=b$.

Тогда для любой непрерывно дифференцируемой на отрезке $[a,b]$ функции $f(x)$
и интерполяционного рационального сплайна $R_{N,k}(x)=R_{N,k} (x; f)$ при
любом натуральном $k$ и всех $x\in [a,b]$ выполняются неравенства
$$
|f(x)-R_{N, k}(x)|\leqslant 16\, \|\Delta\| \,\omega (\|\Delta\|, f^\prime),
$$
$$
|f^\prime (x)-R^\prime_{N,k}(x)|\leqslant (20k+14) \omega (\|\Delta\|, f^\prime).
$$
\end{theorem}


Приведем также оценку скорости сходимости рациональных сплайнов для
 дважды непрерывно дифференцируемых функций.

\begin{theorem}\label{teor2.3}  Для произвольной сетки узлов $ \Delta: a=x_0<x_1<\dots<x_N=b$
 $(N \geqslant 2)$, любой дважды непрерывно дифференцируемой на отрезке $[a, b]$ функции $f(x)$
и интерполяционного рационального сплайна
$R_{N, k}(x)=R_{N, k} (x; f)$ при любом натуральном $k$ и всех
$x\in [a,b]$ выполняется неравенство
$$
|f(x)-R_{N,k}(x)|\leqslant 6\, \|\Delta\|^2
\max_{a\leqslant x \leqslant b}|f^{\prime\prime}(x)|.
$$
\end{theorem}



\section{Сплайны по 4--точечным рациональным интерполянтам}


Пусть функция $f(x)$ непрерывна на отрезке $[a,b]$, на котором задана
некоторая сетка узлов $\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 3)$.

Тогда для любой четверки узлов $x_{k-2}<x_{k-1}<x_k<x_{k+1}$ $(k=2,3,\dots, N-1)$
существует непрерывная на отрезке $[x_{k-2}, x_{k+1}]$ рациональная функция вида
\begin{equation}\label{3.1}
r_k(x)=a_k+b_k(x-x_k)+c_k(x-x_{k-1})(x-x_k)+\frac{A_k}{x-u_k}
\end{equation}
такая, что
$$
r_k(x_j)=f(x_j)\quad (j=k-2,k-1,k,k+1)
$$
(полюс $u_k$ определяется только узлами $x_j$ $(j=k-2, k-1, k,k+1)$),
а коэффициенты для одной и той же четверки узлов могут иметь разные выражения
через разделенные разности функции $f(x)$   в этих узлах.

В частности, выполняются равенства
\begin{equation}
\begin{array}{l}
A_k=-f(x_{k-2}, x_{k-1}, x_k, x_{k+1}) \prod\limits_{j=-2}^1 (x_{k+j}-u_k),\\
a_k=f(x_k)-\dfrac{A_k}{x_k-u_k},\\
c_k=f(x_{k-1}, x_k, x_{k+1})-\dfrac{A_k}{(x_{k-1}-u_k)(x_k-u_k)(x_{k+1}-u_k)},\\
b_k=f(x_{k-1}, x_{k+1})-c_k(x_{k+1}-x_k)+\dfrac{A_k}{(x_{k-1}-u_k)(x_{k+1}-u_k)}.
\end{array}\label{3.2}
\end{equation}

При тех же значениях $A_k$ и $a_k$ два других коэффициента могут задаваться равенствами
\begin{equation}\label{3.3}
\begin{array}{l}
c_k=f(x_{k-2}, x_{k-1}, x_k)-\dfrac{A_k}{(x_{k-2}-u_k)(x_{k-1}-u_k)(x_k-u_k)},\\
b_k=f(x_{k-2}, x_k)+c_k(x_{k-1}-x_{k-2})-\dfrac{A_k}{(x_{k-2}-u_k)(x_k-u_k)}.
\end{array}
\end{equation}

Ради краткости всюду ниже будем пользоваться также обозначениями:
$$
 \alpha_k=\min\{h_{k-1},h_k, h_{k+1}\},\quad
\beta_k=\max \{h_{k-1},h_k, h_{k+1}\}\quad (k=1,2,\dots,N-1);
$$
$$
g_k=\begin{cases} \max\{h_{k-1}, h_k\}, \text{ если } h_{k-1}<h_{k+1},\\
\max\{h_k, h_{k+1}\}, \text{ если } h_{k+1}\leqslant h_{k-1}.
\end{cases}
$$

\begin{lemma}\label{lemma3.1}
Пусть функция $f(x)$ непрерывна на отрезке $[a,b]$, на котором задана сетка
$\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 3)$, и пусть для данного $k$
$(k=1,2,\dots,N-1)$ рациональная функция $r_k(x)$ из равенства \eqref{3.1}
имеет коэффициенты \eqref{3.2} и полюс $u_k=x_{k-2}-g_k$, если
$h_{k-1}<h_{k+1}$, и имеет коэффициенты \eqref{3.3} и полюс $u_k=x_{k+1}+g_k$, если
$h_{k+1}\leqslant h_{k-1}$.

Тогда при $x\in [x_{k-2}, x_{k+1}]$ выполняется неравенство
$$
|r_k(x)-f(x)|\leqslant 38\,\frac{\beta_k}{\alpha_k}\,\omega(\beta_k, f).
$$
\end{lemma}

В нижеследующих двух леммах считаем, что для данной произвольной сетки
$\Delta: a=x_0<x_1<\dots<x_N=b$ при данном $k$ $(k=2,3,\dots,N-1)$
рациональная функция $r_k(x)$ из равенства \eqref{3.1} имеет коэффициенты
\eqref{3.2} и полюс $u_k=x_{k-2}-g_k$, если
$h_{k-1}<h_{k+1}$, и имеет коэффициенты \eqref{3.3} и полюс $u_k=x_{k+1}+g_k$, если
$h_{k+1}\leqslant h_{k-1}$.

\begin{lemma}\label{lemma3.2}
Если  $f\in C^{(1)}[a,b]$ и дана сетка $\Delta: a=x_0<x_1<\dots<x_N=b$
$(N\geqslant 3)$, то для любого $k=2,3,\dots,N-1$ при $x\in [x_{k-2}, x_{k+1}]$
выполняются неравенства
$$
|r^\prime_k(x)-f^\prime(x)|\leqslant 57\,\omega(\beta_k, f^\prime),
$$
$$
|r_k(x)-f(x)|\leqslant \frac{57}2 \,h\,\omega(\beta_k, f^\prime),
$$
где $h=h_j$, если $x\in [x_{j-1}, x_j]$ для данного $j=k-1, k, k+1$.
\end{lemma}

\begin{lemma}\label{lemma3.3}
Если  $f\in C^{(2)}[a,b]$ и дана сетка $\Delta: a=x_0<x_1<\dots<x_N=b$
$(N\geqslant 3)$, то для любого $k=2,3,\dots,N-1$ при $x\in [x_{k-2}, x_{k+1}]$
выполняются неравенства
$$
|r^{\prime\prime}_k(x)-f^{\prime\prime}(x)|\leqslant 39\,\omega(\beta_k, f^{\prime\prime}),
$$
$$
|r^\prime_k(x)-f^\prime(x)|\leqslant 39\,h\,\omega(\beta_k, f^{\prime\prime}),
$$
$$
|r_k(x)-f(x)|\leqslant \frac{39}2 \,h^2\,\omega(\beta_k, f^{\prime\prime}),
$$
где $h=h_j$, если $x\in [x_{j-1}, x_j]$ для данного $j=k-1, k, k+1$.
\end{lemma}

Пользуясь базовыми 4-точечными рациональными интерполянтами $r_k(x)$, построим
рациональные сплайны.

Сначала функцию $f(x)$ будем считать непрерывной $(b-a)$--периодической. Сетку узлов
 $\Delta: a=x_0<x_1<\dots<x_N=b$ продолжим также $(b-a)$--периодически и в соответствии
с этим распространим определение приведенных выше рациональных интерполянтов $r_k(x)$  на
узлы продолженной сетки.

Как видно из приводимого ниже замечания, случай функции $f(x)$, определенной лишь на самом отрезке $[a,b]$,
рассматривается вполне аналогично и получаемые результаты вполне аналогичны периодическому случаю.

Для каждого $k$ $(k=1,2,\dots,N)$ составим рациональную функцию
\begin{equation}\label{3.14}
\begin{array}{c}
Q_k(x)=r_k(x)+(r_{k-1}(x)-r_k(x))\dfrac{(x_k-x)^2}{(x_k-x_{k-2})(x_k-x_{k-1})}+\\
+(r_{k+1}(x)-r_k(x))\dfrac{(x-x_{k-1})^2}{(x_{k+1}-x_{k-1})(x_k-x_{k-1})},
\end{array}
\end{equation}
непрерывную на $[x_{k-1}, x_k]$, причем $Q_k(x_j)=f(x_j)$ при $j=k-1,k$.

Рассмотрим непрерывную на отрезке $[a,b]$ кусочно--рациональную функцию
$\rho_N(x)=\rho_N(x;f)$ $(N=3,4,\dots)$ такую, что при $x\in[x_{k-1}, x_k]$
$(k=1,2,\dots,N)$ выполняется равенство $\rho_N(x)=Q_k(x)$.

Следующие равенства (при $k=1,2,\dots,N-1)$ можно проверить, непосредственно вычисляя производные
$Q^\prime_k(x)$ и $Q_k^{\prime\prime}(x)$:
\begin{equation}\label{3.15}
\begin{array}{l}
\rho^\prime_N(x_k-0)=Q_k^\prime(x_k)=Q^\prime_{k+1}(x_k)=\rho^\prime_N(x_k+0),\\
\rho_N^\prime (x_k)=r^\prime_k(x_k)\dfrac{h_{k+1}}{h_k+h_{k+1}}+r_{k+1}^\prime (x_k)
\dfrac{h_k}{h_k+h_{k+1}};
\end{array}
\end{equation}

\begin{equation}\label{3.16}
\begin{array}{l}
\rho^{\prime\prime}_N(x_k-0)=Q_k^{\prime\prime}(x_k)=Q^{\prime\prime}_{k+1}(x_k)=
\rho^{\prime\prime}_N(x_k+0),\\
\rho_N^{\prime\prime} (x_k)=r^{\prime\prime}_k(x_k)\dfrac{h_{k+1}}{h_k+h_{k+1}}+
r_{k+1}^{\prime\prime} (x_k)\dfrac{h_k}{h_k+h_{k+1}}+\\
\qquad\qquad \qquad\qquad \qquad \qquad +(r^\prime_{k+1}(x_k)-r^\prime_k(x_k)) \dfrac 4{h_k+h_{k+1}}.
\end{array}
\end{equation}

Следовательно, $\rho_N(x)$ $(N\geqslant 3)$ представляет собой дважды непрерывно дифференцируемую
на отрезке $[a,b]$ функцию с $\rho_N(x_k)=f(x_k)$ $(k=0,1,\dots,N)$
такую, что на частичном отрезке $[x_{k-1}, x_k]$ $(k=1,2,\dots, N)$ совпадает с рациональной функцией $Q_k(x)$.

Исследуя аппроксимативные свойства рациональных сплайнов \linebreak$\rho_N(x)=\rho_N(x; f)$, в приводимых ниже
теоремах \ref{3.1}--\ref{3.3}
будем предполагать, что на отрезке $[a,b]$ задана произвольная сетка узлов
 $\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 3)$, продолженная $(b-a)$--периодически.

Следующее утверждение в принятых выше обозначениях дает оценку скорости сходимости сплайнов по
4--точечным рациональным интерполянтам $r_k(x)$ в случае непрерывной функции.

\begin{theorem}\label{teor3.1}
Для непрерывной $(b-a)$--периодической функции $f(x)$ и рационального сплайна $\rho_N(x)=\rho_N(x;f)$
$(N\geqslant 3)$ при $x\in[a,b]$ выполняется неравенство
$$
|f(x)-\rho_N(x)|\leqslant 38\,\sup\left\{\frac{\beta_k}{\alpha_k}\omega(\beta_k, f):
 k=2,3,\dots, N-1\right\};
$$
в частности,
$$
|f(x)-\rho_N(x)|\leqslant 38\,\frac{\|\Delta\|}{\alpha}\,\omega(\|\Delta\|, f),
$$
где $\alpha=\min\{h_k: k=1, 2,\dots,N\}$.
\end{theorem}

\begin{theorem}\label{teor3.2}
Для непрерывно дифференцируемой $(b-a)$--периодической \linebreak функции $f(x)$ и
 рационального сплайна $\rho_N(x)=\rho_N(x;f)$ $(N\geqslant 3)$ при $x\in[a,b]$ выполняются
неравенства
\begin{equation*}
|f(x)-\rho_N(x)|\leqslant \frac{57}2\, \|\Delta\| \omega(\|\Delta\|, f^\prime),
\end{equation*}
\begin{equation*}
|f^\prime(x)-\rho^\prime_N(x)|\leqslant 285 \,\omega(\|\Delta\|, f^\prime).
\end{equation*}
\end{theorem}

\begin{theorem}\label{teor3.3}
Для дважды непрерывно дифференцируемой $(b-a)$--периоди\-ческой функции $f(x)$ и
 рационального сплайна $\rho_N(x)=\rho_N(x;f)$ $(N\geqslant 3)$ при $x\in[a,b]$ выполняются
неравенства
\begin{equation*}
|f(x)-\rho_N(x)|\leqslant \frac{39}2\, \|\Delta\|^2 \omega(\|\Delta\|, f^{\prime\prime}),
\end{equation*}
\begin{equation*}
|f^{\prime\prime}(x)-\rho^{\prime\prime}_N(x)|\leqslant 429 \,\omega(\|\Delta\|, f^{\prime\prime}).
\end{equation*}
\end{theorem}

{\bf Замечание о непериодическом случае.} Если функция $f(x)$ определена на отрезке
$[a,b]$, на котором задана сетка узлов $\Delta: a=x_0<x_1<\dots<x_N=b$ $(N\geqslant 3)$,
то 4--точечные рациональные интерполянты $r_k(x)$ и их коэффициенты при
$k=2,3,\dots<N-1$ определяем, как и выше, по формулам \eqref{3.1}--\eqref{3.3}.

Интерполянты $r_k(x)$ для концевых пар частичных отрезков, т.е. для значений $k=0,1$
и $k=N,N-1$, определим равенствами $r_0(x)=r_1(x)=r_2(x)$ $(x\in[x_0, x_3])$ и
$r_{N+1}(x)=r_N (x)=r_{N-1}(x)$ $(x\in [x_{N-3}, x_N])$.

Промежуточные рациональные функциии $Q_k(x)$ при всех $k=1,2,\dots,N$
определяются по вышеприведенной формуле \eqref{3.14}.
Определение рационального сплайна $\rho_N(x)=\rho_N(x; f)$, его непрерывность и
непрерывность $\rho^\prime_N(x)$ и $\rho^{\prime\prime}_N(x)$ на $[a,b]$
также сохраняются.

При этом значения $\rho^\prime_N(x_k)$ и $\rho^{\prime\prime}_N(x_k)$ при всех
$k=1,2,\dots,N$ вычисляются по общим формулам \eqref{3.15} и \eqref{3.16}.

Все оценки, полученные выше в теоремах \ref{teor3.1}--\ref{teor3.3}, сохраняются
 и в непериодическом случае.



%=================Список литературы====================
\end{fulltext}
\begin{thebibliography}{99}

\bibitem{1} Алберг Дж., Нилсон Э., Уолш Дж. Теория сплайнов и ее приложения. М., Мир. 1972.
%\RBibitem{1}
%\by Дж.~Алберг, Э.~Нилсон, Дж.~Уолш
%\book Теория сплайнов и ее приложения
%\publaddr М.
%\publ Мир
%\yr 1972

\bibitem{2} Стечкин~С.В., Субботин~Ю.Н. Добавления к книге Дж.~Алберг, Э.~Нилсон, Дж.~Уолш. Теория сплайнов и ее приложения. М., Мир. 1972.
%\RBibitem{2}
%\by С.\,Б.~Стечкин, Ю.\,Н.~Субботин
%\book Добавления к книге Дж.~Алберг, Э.~Нилсон, Дж.~Уолш. Теория сплайнов и ее приложения
%\publaddr М.
%\publ Мир
%\yr 1972

\bibitem{3}Стечкин~С.В., Субботин~Ю.Н. Сплайны в вычислительной математике. М. Наука. 1976.

%\RBibitem{3}
%\by С.\,Б.~Стечкин, Ю.\,Н.~Субботин
%\book Сплайны в вычислительной математике
%\publaddr М.
%\publ Наука
%\yr 1976

\bibitem{4} Корнейчук~Н.В. Сплайны в теории приближения. М., Наука. 1984.

%\RBibitem{4}
%\by Н.\,П.Корнейчук
%\book Сплайны в теории приближения
%\publaddr М.
%\publ Наука
%\yr 1984

\bibitem{5}Черных~Н.И. Приближение сплайнами с заданной плотностью распределения узлов // Тр. МИАН СССР. 1975. Vol. 138. М. С. 174--197.

%\RBibitem{5}
%\by Н.\,И.~Черных
%\paper Приближение сплайнами с заданной плотностью распределения узлов
%\inbook Тр. МИАН СССР
%\vol 138
%\publaddr М.
%\publ
%\yr 1975
%\pages 174--197

\bibitem{6}Малоземов В.Н., Певный А.Б. Полиномиальные сплайны. Л. Изд-во ЛГУ. 1986.

%\RBibitem{6}
%\by В.\,Н.~Малоземов, А.\,Б.~Певный
%\book Полиномиальные сплайны
%\publaddr Л.
%\publ Изд-во ЛГУ
%\yr 1986

\bibitem{7}Завьялов Ю.С., Квасов Б.И., Мирошниченко В.Л. Методы сплайн--функций. М., Наука. 1980.

%\RBibitem{7}
%\by Ю.\,С.~Завьялов, Б.\,И.~Квасов, В.\,Л.~Мирошниченко
%\book Методы сплайн--функций
%\publaddr М.
%\publ Наука
%\yr 1980

\bibitem{8} Nord S. Approximation properties of the spline fit // BIT. 1967. Vol. 7. Pp. 132--144.

%\RBibitem{8}
%\by S.~Nord
%\paper Approximation properties of the spline fit
%\inbook BIT
%\vol 7
%\publaddr
%\publ
%\yr 1967
%\pages 132--144

\bibitem{9}Привалов Ал.А. О сходимости кубических интерполяционных сплайнов к непрерывной функции // Матем. заметки. 1979. Т.25. Вып. 5. С. 681--700.

%\RBibitem{9}
%\by Ал.\,А.~Привалов
%\paper О сходимости кубических интерполяционных сплайнов к непрерывной функции
%\inbook Матем. заметки.
%\vol 25:5
%\publaddr М.
%\publ
%\yr 1979
%\pages 681--700

\bibitem{10}Schaback R. Spezielle rationale Splinefunktionen // J. Approx. Theory. 1973. Vol. 7. Issue 2. Pp. 281--292.

%\RBibitem{10}
%\by R.~Schaback
%\paper Spezielle rationale Splinefunktionen
%\inbook J. Approx. Theory
%\vol 7:2
%\publaddr
%\publ Academic Press, Inc.
%\yr 1973
%\pages 281--292

\bibitem{11} Duan Q., Djidjeli K., Price W.G., Twizell E.H. Weighted rational  cubic spline interpolation and its application // J. of Computational  and Applied Mathematics. 2000. Vol. 117. Issue 2. Pp. 121--135.


%\RBibitem{11}
%\by Q.~Duan, K.~Djidjeli, W.\,G.~Price, E.\,H.~Twizell
%\paper Weighted rational  cubic spline interpolation and its application
%\inbook J. of Computational  and Applied Mathematics
%\vol 117:2
%\publaddr
%\publ
%\yr 2000
%\pages 121--135

\bibitem{12} Oja P. Rational spline interpolation to monotonic data // Proceed. of the Estonian Acad. of Sciences. Physics*Mathematics. 1999. Vol. 48. Issue 1. Pp. 22--30.

%\RBibitem{12}
%\by P.~Oja
%\paper Rational spline interpolation to monotonic data
%\inbook Proceed. of the Estonian Acad. of Sciences. Physics*Mathematics
%\vol 48:1
%\publaddr Tartu
%\publ
%\yr 1999
%\pages 22--30

\bibitem{13}Tian M., Geng H.L. Error analysis of a rational interpolation spline // Intern. J. of Mathematical Analysis. Vol. 5. Issue 25--28. Pp. 1287--1294.

%\RBibitem{13}
%\by M.~Tian and H.\,L.~Geng
%\paper Error analysis of a rational interpolation spline
%\inbook Intern. J. of Mathematical Analysis
%\vol 5: 25--28
%\publaddr
%\publ
%\yr 2011
%\pages 1287--1294

\bibitem{14}Hussain M.Z., Sarfraz M., Shaikh T.S. Shape preserving rational cubic spline for positive and convex data // Egyptian Informatics Journal. 2011. Vol. 12. Pp. 231--236.

%\RBibitem{14}
%\by M.\,Z.~Hussain, M.~Sarfraz, T.\,S.~Shaikh
%\paper Shape preserving rational cubic spline for positive and convex data
%\inbook Egyptian Informatics Journal
%\vol 12
%\publaddr
%\publ
%\yr 2011
%\pages 231--236

\bibitem{15}Edeoa A. Gofeb G., Tefera T. Shape preserving $C^2$ rational cubic spline interpolation // American Scientific Research Journal for Engineering, Technology and Sciences. 2015. Vol. 12. Issue 1. Pp. 110--122.

%\RBibitem{15}
%\by A.~Edeoa, G.~Gofeb, T.~Tefera
%\paper Shape preserving $C^2$ rational cubic spline interpolation
%\inbook American Scientific Research Journal for Engineering, Technology and Sciences
%\vol 12:1
%\publaddr
%\publ
%\yr 2015
%\pages 110--122

\bibitem{16}Субботин Ю.Н. Вариации на тему сплайнов // Фундамент. и прикл. матем. 1997. Т.3. Вып. 4. С. 1043--1058.

%\RBibitem{16}
%\by Ю.\,Н.~Субботин
%\paper Вариации на тему сплайнов
%\inbook Фундамент. и прикл. матем.
%\vol 3:4
%\publaddr М.
%\publ Центр новых информационных технологий МГУ, Издательский дом "Открытые системы"
%\yr 1997
%\pages 1043--1058


\end{thebibliography}
\end{document}
